export enum CollectionEnum {
    USERS = "users",
    PLACES = "places",
}

export enum Queues {
    EMAILS = "emails",
    AI = "ai",
}

export enum Tasks {
    NEWSLETTER = "newsletter",
    PLACE_EMBEDDING = "place_embedding",
}
